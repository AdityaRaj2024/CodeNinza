#include<stdio.h>
void main()
{
    //To Remove Duplicate element from array
    /*int size,i,j,s;
	printf("Enter Size Of Array =");
	scanf("%d",&size);
	int n[size];
    printf("Enter %d Values :", size);
	for (i=0;i<size;i++)
		scanf("%d",&n[i]);
    for(i=0;i<size;i++)
    {
        for(j=i+1;j<size;j++)
        {
            if(n[i]==n[j])
            {
                for(s=j;s<size;s++)
                    n[s]=n[s+1];
            
            j--;
            size--;
            }
        }
    }
    for(i=0;i<size;i++)
        printf("%d\t",n[i]);*/

    //WAP to input n numbers and display all pairs of elements whose sum is 50.
    /*int size,i,j,s;
	printf("Enter Size Of Array =");
	scanf("%d",&size);
	int n[size];
    printf("Enter %d Values :", size);
	for (i=0;i<size;i++)
		scanf("%d",&n[i]);
    for(i=0;i<size;i++)
    {
        for(j=i+1;j<size;j++)
        {
            if(n[i]+n[j]==50)
            {
               printf("%d\t%d\n",n[i],n[j]);
            }
        }
    }*/
   
   
}