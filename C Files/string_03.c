#include<stdio.h>
#include<string.h>
#define size 7
void main()
{
    //wap to input n string after that print it.
    /*char name[size][30];
    int i;
    printf("Enter %d String =\n",size);
    for(i=0;i<size;i++)
        gets(name[i]);
        //scanf("%[^\n]",name[i]);
    printf("All String Are =\n");
    for(i=0;i<size;i++)
        printf("%s\n",name[i]);*/

    //wap to input n string after that find length of each string.
    /*char name[size][30];
    int i;
    printf("Enter %d String =\n",size);
    for(i=0;i<size;i++)
        gets(name[i]);
    printf("Length Of Each String Are =\n");
    for(i=0;i<size;i++)
    {
        printf("%s\t%d\n",name[i],strlen(name[i]));
    }*/
}